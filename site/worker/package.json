{
  "name": "mainguard-site-api",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for the Mainguard marketing site (waitlist + contact forms).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
