<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="GitLoom.App.Views.DeviceFlowAuthDialog"
        Title="GitHub Authorization"
        Width="450" Height="250"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        Background="{DynamicResource SurfacePanel}">
    
    <Grid RowDefinitions="Auto, *, Auto" Margin="20">
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
            <PathIcon Data="{StaticResource GitHubIcon}" Width="24" Height="24" Foreground="{DynamicResource TextPrimary}"/>
            <TextBlock Text="Connect to GitHub" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="10" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <TextBlock Text="1. Your browser will open automatically." Foreground="{DynamicResource TextPrimary}" FontSize="14" TextAlignment="Center"/>
            <TextBlock Text="(If it didn't, click the URL below:)" Foreground="{DynamicResource TextMuted}" FontSize="12" Margin="0,-5,0,0" TextAlignment="Center"/>
            <SelectableTextBlock x:Name="UrlText" Text="https://github.com/login/device" Foreground="{DynamicResource AccentBrush}" FontSize="14" FontWeight="SemiBold" Cursor="Hand" PointerPressed="Url_PointerPressed" TextAlignment="Center"/>
            
            <TextBlock Text="2. The following code was copied to your clipboard!" Foreground="{DynamicResource TextPrimary}" FontSize="14" Margin="0,10,0,0" TextAlignment="Center"/>
            <Border Background="{DynamicResource SurfaceCard}" BorderBrush="{DynamicResource BorderHairline}" BorderThickness="1" CornerRadius="8" Padding="10" HorizontalAlignment="Center">
                <Grid ColumnDefinitions="*, Auto" Width="250">
                    <SelectableTextBlock x:Name="CodeText" Grid.Column="0" Text="XXXX-YYYY" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Copy Again" Click="CopyCode_Click" Background="Transparent" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </StackPanel>

        <Button Grid.Row="2" Content="Cancel" Click="Cancel_Click" Background="Transparent" Foreground="{DynamicResource TextMuted}" HorizontalAlignment="Right"/>
    </Grid>
</Window>
