# GitLoomOS payload — package set (P2-21 invariant 2: tarball hash-stable given pinned inputs).
#
# Versions are NOT pinned per-line. Determinism comes from pinning apt at a frozen
# snapshot.debian.org timestamp (see DEBIAN_SNAPSHOT in the Dockerfile): the snapshot freezes the
# whole bookworm archive, so installing a package *name* yields the one deterministic version that
# existed at that instant. That is both bit-reproducible AND always fetchable — exact per-version
# pins used to rot the moment Debian retired an old version from the mirrors.
#
# To move the version floor, bump DEBIAN_SNAPSHOT in the Dockerfile (see docs/gitloomos-updates.md).
#
# format: one bookworm (main/security) package name per line; comments/blank lines ignored.
#
# --- container runtime (the sandbox substrate P2-07 depends on) ---
docker.io
containerd
# --- git + transport ---
git
ca-certificates
openssh-client
# --- .NET runtime dependency: gitloomd is a self-contained linux-x64 .NET app and ABORTS on startup
#     ("Couldn't find a valid ICU package installed on the system", SIGABRT) without ICU. Shipping
#     libicu keeps full globalization (no behaviour change), vs forcing InvariantGlobalization. ---
libicu72
# --- pre-baked agent toolchain (P2-07 A6-clean: no runtime egress to add tools) ---
jq
ripgrep
fd-find
make
# --- node/npm: the INSTALL HOST for the dynamic agent CLIs (P2-22 §J-5). The adapter channel installs
#     each user-chosen CLI in-VM with `npm install -g --prefix /home/gitloom/gitloom/adapters <staged
#     tarball>`, so npm must exist in the VM. This is the install side ONLY — agents RUN the CLIs inside
#     the jail against the agent image's own node 20 (/opt/toolchain, Nix-pinned), reached via the
#     read-only /opt/gitloom/adapters bind mount. bookworm's node 18 emits EBADENGINE warnings for CLIs
#     declaring node>=22 but installs them correctly (verified: claude-code/codex/opencode); the runtime
#     node is what must satisfy the engine, and it does. ---
nodejs
npm
# --- base hardening / init ---
sudo
tzdata
# --- base sysadmin tools (procps=ps/sysctl, iproute2=ip): FirstBootStep reads/writes /proc/sys
#     directly so it doesn't depend on these, but agents + diagnostics expect them present. ---
procps
iproute2
# --- init: systemd, so WSL `[boot] systemd=true` supervises the gitloomd unit (P2-05 StartDaemonStep
#     uses `systemctl start gitloomd`) and docker. systemd-sysv provides /sbin/init.
#     dbus + libpam-systemd are NOT optional with systemd=true: logind/user sessions need them. Without
#     dbus every `wsl -d GitLoomEnv -u root` fails to start a systemd user session ("Failed to start the
#     systemd user session for 'root'"), and a burst of those (the first-boot Docker poll) drives the WSL
#     service into Wsl/Service/E_UNEXPECTED. ---
systemd
systemd-sysv
dbus
libpam-systemd
