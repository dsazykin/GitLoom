using Avalonia.Controls;

namespace Mainguard.Agents.UI.Views;

/// <summary>Settings → Mainguard OS: the repo-onboarding engine + Rebuild sandbox images, embedded
/// as a page (replaces the standalone "Add Repos to Mainguard OS" window).</summary>
public partial class MainguardOsPageView : UserControl
{
    public MainguardOsPageView()
    {
        InitializeComponent();
    }
}
