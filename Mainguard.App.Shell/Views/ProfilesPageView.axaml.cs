using Avalonia.Controls;

namespace Mainguard.App.Shell.Views;

/// <summary>Settings → Git Profiles, embedded as a page (was a standalone dialog).</summary>
public partial class ProfilesPageView : UserControl
{
    public ProfilesPageView()
    {
        InitializeComponent();
    }
}
